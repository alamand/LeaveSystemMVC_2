@model LeaveSystemMVC.Models.sEmployeeModel

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>


@using (Html.BeginForm())
{
    // @Html.AntiForgeryToken()
    //Below should not be empstartdate.... need to ask ma'am what to do here

    <div class="form-group">
        @Html.LabelFor(model => model.email,"Upload CSV File", htmlAttributes: new { @class = "control-label col-md-2" })
        <div class="col-md-10">
            @Html.TextBoxFor(model => model.email, new { type = "file" })
            @Html.ValidationMessageFor(model => model.email, "", new { @class = "text-danger" })
            <br />
            <!-- <input type="submit" value="Upload" name=" = " upload" />
            -->
        </div>
    </div>
    <div class="form-horizontal">
        <div class="col-md-offset-2 col-md-10">
            <input type="submit" value="Submit" class="btn btn-default" />
        </div>
    </div>

}
@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
